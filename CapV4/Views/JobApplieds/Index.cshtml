@model IEnumerable<CapModel.JobApplied>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

@*<p>
    @Html.ActionLink("Create New", "Create")
</p>*@
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ApplicationDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AppliedMessage)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.JobPosting.JobTitle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.JobSeeker.SkillSummary)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ApplicationDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AppliedMessage)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.JobPosting.JobTitle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.JobSeeker.SkillSummary)
        </td>
        <td>
            @*@Html.ActionLink("Edit", "Edit", new { id=item.JobAppId }) |*@
            @Html.ActionLink("Details", "Details", new { id=item.JobAppId }) 
            
            @*@Html.ActionLink("Delete", "Delete", new { id=item.JobAppId })*@
        </td>
    </tr>
}

</table>
@Html.ActionLink("Back", "Index", "Recruiters" )
